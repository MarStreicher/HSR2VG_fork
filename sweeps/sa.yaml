program: src/train.py
method: bayes
name: sa-maize-soybean
entity: user
project: hsr
run_cap: 100
command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args}
  - --use_wandb
  - --model=SA
  - --label=LMA
  - --source_domain=maize
  - --target_domain=soybean
  - --sweep
metric:
  name: source_r
  goal: maximize
parameters:
  n_components:
    values: [2, 10, 20, 30, 40, 50, 60, 70, 80, 85, 90, 100, 200, 300, 500]
  sa_n_component:
    values: [2, 10, 20, 30, 40, 50, 60, 70, 80, 85, 90, 100, 200, 300, 500]
